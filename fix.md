这份整理是专门为 Code Agent（如 Cursor, GitHub Copilot 等）编写的 Prompt 结构。我将需求拆解为了具体的**组件级**和**页面级**的 Action 项，方便 AI 直接理解并生成代码。

你可以直接复制以下内容发给你的 Code Agent：

---

### 潍坊科技学院图书馆系统 - 前端/UI 修改需求文档

请根据以下具体的模块划分，修改和重构当前项目的代码：

#### 1. 全局配置与路由 (Global & Routing)

*
**转场动画**：重构或修改各个页面之间切换的跳转/转场动画 (Transition Animations)，使其更加平滑 。


*
**路由守卫/Bug修复**：修复 Android 物理返回键导致的 Bug。当前在首页不登录时，使用安卓自带的返回键会卡住，或者跳转到已登录界面，需完善未登录状态下的路由拦截和回退逻辑 。



#### 2. 公共布局组件 (Layout Components)

* **顶部导航栏 (Header)**：在“空间预约”页面，将原本靠左的标题文案 `study-room` 修改为中文 `图书馆预约`，并设置为**居中对齐**。
* **底部导航栏 (Bottom Tab Bar)**：全局替换现有的三个 Tab（首页、当前预约、我的中心），改为全新的五项 Tab 结构：`首页`、`发布`、`我的课表`、`消息`、`我的`。

#### 3. 空间预约与筛选页 (Reservation & Filter)

* **日期选择器 (Date Selector)**：移除现有的手动输入日期弹窗组件。改为直接在界面横向平铺展示快捷选项：固定显示 `今天` 和 `明天` 的快捷按钮。
* **筛选面板 (Filter Menu)**：将居中弹出的筛选对话框，修改为侧边滑出或底部弹出的抽屉组件 (Drawer/Bottom Sheet)，内部包含“馆舍”、“楼层”、“座位类型”等多选逻辑。

#### 4. 座位平面图选座页 (Seat Map Area)

* **容器布局**：去除平面图区域上方的多余空白 padding/margin，最大化座位平面图的渲染显示面积。
* **手势交互重构**：
* 移除界面上现有的点击式 `+` 和 `-` 缩放按钮。
* 修复画布/地图目前无法滑动的 Bug，使其支持单指拖拽滑动 (Pan)。
* 增加双指捏合缩放 (Pinch-to-zoom) 的手势支持。


* **新增收藏功能**：在用户选中座位的弹窗或状态栏中，新增一个“收藏”图标 (Icon)，点击可将该座位加入用户的“常用座位”列表。

#### 5. 登录、绑定与首页排版 (Login & Home)

*
**登录页 UI**：将左上角的返回按钮 (`<`) 样式整体向上移动，调整其 Top 边距 。


*
**弹窗美化**：重构账号“绑定成功”时的 Modal/Dialog 提示框样式（优化圆角、内边距、阴影及按钮样式） 。


*
**首页排版 (Home)**：缩小首页顶部图片/Banner 的高度，解决目前图片过大导致的排版比例问题 。



#### 6. 扩展业务功能模块 (Extended Features)

*
**常用座位列表**：重写“常用”列表的 Item 样式，采用卡片式分条展示（需包含：日期、时间、地点、座位号及右侧的“预约”按钮） 。


*
**课表查询 (Timetable)**：在此页面新增“学期选择”控件（如下拉菜单），并增加“假期显示”的渲染逻辑 。


*
**教务功能新增**：增加两个全新的页面路由及入口：`选课` 系统和 `成绩查询` 系统 。
